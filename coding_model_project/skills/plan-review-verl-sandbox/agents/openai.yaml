interface:
  display_name: "Plan Review (verl+sandbox)"
  short_description: "Strictly review implementation plans for verl/SandboxFusion."
  default_prompt: "Review this implementation plan (not high-level design), verify verl and SandboxFusion API/command/path claims, and enforce phase key metric coverage based on eval_protocol.md and metric_templates.md."

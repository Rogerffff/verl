#!/bin/bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

STATE_ROOT=${STATE_ROOT:-/root/sandboxfusion-step400}
BASE_PORT=${BASE_PORT:-8097}
BACKEND_COUNT=${BACKEND_COUNT:-4}
MAX_BACKENDS=${MAX_BACKENDS:-4}
LB_BASE_PORT=${LB_BASE_PORT:-8096}
LB_COUNT=${LB_COUNT:-1}
BACKENDS_PER_LB=${BACKENDS_PER_LB:-4}
PROBE_REQUESTS=${PROBE_REQUESTS:-8}
PROBE_WORKERS=${PROBE_WORKERS:-4}

exec env \
  STATE_ROOT="$STATE_ROOT" \
  BASE_PORT="$BASE_PORT" \
  BACKEND_COUNT="$BACKEND_COUNT" \
  MAX_BACKENDS="$MAX_BACKENDS" \
  LB_BASE_PORT="$LB_BASE_PORT" \
  LB_COUNT="$LB_COUNT" \
  BACKENDS_PER_LB="$BACKENDS_PER_LB" \
  PROBE_REQUESTS="$PROBE_REQUESTS" \
  PROBE_WORKERS="$PROBE_WORKERS" \
  bash "$SCRIPT_DIR/setup_eval_sandbox_4x2.sh"

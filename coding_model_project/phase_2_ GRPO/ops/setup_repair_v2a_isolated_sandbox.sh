#!/bin/bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

STATE_ROOT=${STATE_ROOT:-/root/sandboxfusion-step200}
BASE_PORT=${BASE_PORT:-8085}
BACKEND_COUNT=${BACKEND_COUNT:-2}
MAX_BACKENDS=${MAX_BACKENDS:-2}
LB_BASE_PORT=${LB_BASE_PORT:-8094}
LB_COUNT=${LB_COUNT:-1}
BACKENDS_PER_LB=${BACKENDS_PER_LB:-2}
PROBE_REQUESTS=${PROBE_REQUESTS:-6}
PROBE_WORKERS=${PROBE_WORKERS:-2}

exec env \
  STATE_ROOT="$STATE_ROOT" \
  BASE_PORT="$BASE_PORT" \
  BACKEND_COUNT="$BACKEND_COUNT" \
  MAX_BACKENDS="$MAX_BACKENDS" \
  LB_BASE_PORT="$LB_BASE_PORT" \
  LB_COUNT="$LB_COUNT" \
  BACKENDS_PER_LB="$BACKENDS_PER_LB" \
  PROBE_REQUESTS="$PROBE_REQUESTS" \
  PROBE_WORKERS="$PROBE_WORKERS" \
  bash "$SCRIPT_DIR/setup_eval_sandbox_4x2.sh"
